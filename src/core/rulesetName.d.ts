/**
 * Type declarations for `rulesetName.js` (Devvit `allowJs: false` boundary).
 * Keep in step with the JSDoc next door.
 */

/** Deterministic two-word mnemonic for a 32-char ruleset hex. */
export function rulesetName(hexCode: string): string
